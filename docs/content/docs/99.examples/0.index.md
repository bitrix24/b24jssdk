---
title: Examples
description: 'Runnable cookbook recipes and a catalogue of SDK-native scripts that show how to combine the SDK into real apps.'
audited: 2026-05-29
navigation:
  title: Introduction
links: []
---

::warning
These recipes are an early preview. Each one is type-checked against the public SDK surface, but **end-to-end runtime verification against a live portal is still in progress** ([issue #60](https://github.com/bitrix24/b24jssdk/issues/60)). Review the per-recipe `Limitations` (or `Notes`) block before using one in production.
::

## Cookbook (curated entry points)

Each cookbook recipe is a single, copy-paste-friendly file with environment variables, an expected output sketch, and a `Limitations` block. Pick the one closest to what you're building and adapt.

::card-group
  ::card
  ---
  title: Webhook CLI smoke test
  to: /docs/examples/webhook-cli-node
  ---
  Verify a freshly created inbound webhook in 30 lines. Always start here.
  ::

  ::card
  ---
  title: Export deals to CSV
  to: /docs/examples/dashboard-deals-csv
  ---
  Stream every deal in a date window into a CSV using `FetchListV2.make()`. Memory stays flat regardless of result size.
  ::

  ::card
  ---
  title: Bulk update deals
  to: /docs/examples/bulk-update-deals
  ---
  Migrate thousands of deals to a new stage using `BatchByChunkV2.make()` with partial-error handling.
  ::

  ::card
  ---
  title: Frame app skeleton
  to: /docs/examples/frame-app-skeleton
  ---
  A minimum viable Vue 3 iframe app: handshake, parent title, persisted options, slider open/close.
  ::

  ::card
  ---
  title: Subscribe to Pull events
  to: /docs/examples/pull-subscribe-frame
  ---
  Live events in a frame app via `useB24Helper` and the Pull client (WebSocket + long-polling).
  ::
::

## Extended catalogue

A broader set of end-to-end scripts on the canonical `$b24.actions.v{2,3}.*.make()` surface. Each one is a single TypeScript file you run with `tsx` after setting `B24_HOOK` to your incoming webhook URL. The source `.ts` files also live under `.claude/skills/b24jssdk-recipes/examples/` so that AI agents can read them as authoritative templates.

| # | Page | Stack | Bitrix24 scopes |
|---|---|---|---|
| 1 | [CRM analytics — sales funnel](/docs/examples/crm-analytics) | Node | `crm` |
| 2 | [Mass messaging](/docs/examples/mass-messaging) | Node | `crm`, `im` |
| 3 | [Task automation on stage transitions](/docs/examples/task-automation) | Node | `crm`, `task` |
| 4 | [ERP / 1C contact sync](/docs/examples/erp-sync) | Node, `node-cron` | `crm` |
| 5 | [Disk: storages, folders, files](/docs/examples/disk-files) | Node | `disk` |
| 6 | [Telegram bot for new deals](/docs/examples/telegram-bot) | Node, `grammy`, `node-cron` | `crm` |
| 7 | [Outbound webhook handler](/docs/examples/webhook-handler) | Node, `express` | `crm` |
| 8 | [AI assistant: analyse a deal, create a task](/docs/examples/ai-assistant) | Node, `openai` | `crm`, `task` |
| 9 | [Web search + LLM with timeline write-back](/docs/examples/web-search-llm) | Node, BYOC | `crm` |
| 10 | [Error-handling cookbook](/docs/examples/error-handling) | Node | any |
| 11 | [Outbound event registration](/docs/examples/event-registration) | Node | `crm` |
| 12 | [OAuth install handshake](/docs/examples/oauth-install) | Node, `express` | OAuth app |

## Common boot snippet

Every extended-catalogue recipe starts with the same loader:

```ts
import { B24Hook, Logger, ConsoleV2Handler, LogLevel, type TypeB24 } from '@bitrix24/b24jssdk'

export function bootB24(): TypeB24 {
  const url = process.env.B24_HOOK
  if (!url) throw new Error('B24_HOOK env var is required')
  const $b24 = B24Hook.fromWebhookUrl(url)
  $b24.offClientSideWarning()
  return $b24
}

export const logger = Logger.create('Recipe')
logger.pushHandler(new ConsoleV2Handler(LogLevel.INFO, { useStyles: false }))
```

Functions inside each recipe accept `$b24: TypeB24`, so the same code runs unchanged with `B24Frame` (in-iframe app) or `B24OAuth` (OAuth-installed app). Only the boot step changes.

## API-version split

| Method | Action surface |
|---|---|
| `crm.item.{get,list,add,update,delete}` | `actions.v2.*` |
| `crm.activity.list`, `crm.timeline.comment.add`, `disk.*`, `im.*`, `user.*`, `profile` | `actions.v2.*` |
| `tasks.task.{add,get,update,delete}` | `actions.v3.*` |
| `tasks.task.list` | `actions.v2.*` |
| `main.eventlog.{list,get,tail}` | `actions.v3.*` |

> Calling a v3-eligible method via `actions.v2.*` works but logs a warning (`JSSDK_CORE_METHOD_AVAILABLE_IN_API_V3`). Calling a non-v3 method via `actions.v3.*` throws `SdkError(JSSDK_CORE_METHOD_NOT_SUPPORT_IN_API_V3)`.

## Notes that apply to every extended-catalogue recipe

- CRM `crm.item.*` methods use camelCase fields (`stageId`, `assignedById`, `opportunity`). Tasks (`tasks.task.*`), Disk (`disk.*`), and IM (`im.*`) use classic uppercase fields. Recipes follow this split.
- Stage IDs on multi-funnel portals carry a category prefix (`C2:WON`, `C4:LOSE`). Recipes 1, 3, and 6 strip it via a small `baseStage()` helper.
- `callList.make` / `fetchList.make` **strip user-supplied `order`** — keyset pagination requires `idKey ASC`. Narrow with `filter` instead.
- For `crm.item.list` set `idKey: 'id'` and `customKeyForResult: 'items'`. For classic methods use the default `idKey: 'ID'` (uppercase) and either omit `customKeyForResult` or set it to the right key (`'tasks'`, `'files'`, …).
- Filters use the **v2 prefix** dialect (`'>=opportunity'`, `'!stageId'`, `'%title'`) because every recipe stays on v2 actions for CRM. MongoDB-style `$gt`/`$ne`/`$contains` is a VibeCode-only convention and is **not** valid against Bitrix24 REST.
- Bitrix24 outbound webhooks always need a `200` response — recipe 7 always returns 200, even when the handler throws, so Bitrix24 doesn't retry for 24 h.
