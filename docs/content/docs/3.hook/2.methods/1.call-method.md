---
title: B24Hook.callMethod()
description: 'Основной метод для выполнения вызовов REST API Битрикс24 через вебхук. Позволяет вызывать любой метод REST API с указанными параметрами. Работает только в серверной среде.'
navigation:
title: callMethod
links:
- label: B24Hook
  iconName: GitHubIcon
  to: https://github.com/bitrix24/b24jssdk/blob/main/packages/jssdk/src/hook/controller.ts
- label: TypeB24
  iconName: GitHubIcon
  to: https://github.com/bitrix24/b24jssdk/blob/main/packages/jssdk/src/types/b24.ts
- label: LoggerBrowser
  iconName: GitHubIcon
  to: https://github.com/bitrix24/b24jssdk/blob/main/packages/jssdk/src/logger/browser.ts
- label: Retrieve Large Volumes of Data
  iconName: Bitrix24Icon
  to: https://apidocs.bitrix24.com/settings/performance/huge-data.html
---

**@todo:**
- логирование нормальное
- типизацию проверить
- в коде писл что ещепроверить
- вообще проверить

::caution{title="⚠️ ВНИМАНИЕ: БЕЗОПАСНОСТЬ"}
Объект `B24Hook` предназначен **исключительно для использования на сервере**. Вебхук содержит секретный ключ доступа, его **НЕЛЬЗЯ** использовать в клиентском коде (браузере, мобильном приложении). Для клиентской стороны используйте [`B24Frame`](/docs/frame/).
::

::warning{title="📝 ПРИМЕЧАНИЕ: СТАТУС СТРАНИЦЫ"}
Эта страница находится в разработке. Некоторые детали могут быть уточнены позже.
::


## Краткий обзор

Используйте `callMethod`{lang="ts-type"} для прямых вызовов REST API.
Метод возвращает `Promise` с объектом `AjaxResult`{lang="ts-type"}, содержащим данные ответа, статус и методы для обработки ошибок и пагинации.

```ts
// Базовая схема использования
import { B24Hook } from '@bitrix24/b24jssdk'

const $b24 = B24Hook.fromWebhookUrl('https://your_domain.bitrix24.com/rest/1/webhook_code/')

const result = await $b24.callMethod('метод.api', {параметры})
```

## Сигнатура метода

```ts-type
callMethod<T = unknown>(
  method: string,
  params?: object,
  start?: number
): Promise<AjaxResult<T>>
```

### Параметры

| Параметр     | Тип                      | Обязательный | Описание                                                                                                                                       |
|:-------------|:-------------------------|:-------------|:-----------------------------------------------------------------------------------------------------------------------------------------------|
| **`method`** | `string`{lang="ts-type"} | Да           | Имя вызываемого метода REST API (например, `crm.item.get`).                                                                                    |
| **`params`** | `object`{lang="ts-type"} | Нет          | Объект с параметрами, передаваемыми в метод API.                                                                                               |
| **`start`**  | `number`{lang="ts-type"} | Нет          | **Контроль пагинации.** Смещение для постраничной выборки. По умолчанию `0`. Используйте `-1` для отключения пагинации при массовых выгрузках. |

### Возвращаемое значение

`Promise<AjaxResult<T>>`{lang="ts-type"} — промис, который разрешается объектом [`AjaxResult`{lang="ts-type"}](/docs/core/ajax-result/).
Этот объект предоставляет:
* `.getData(): Payload<T>`{lang="ts-type"} — возвращает полный ответ API.
* `.isSuccess: boolean`{lang="ts-type"} — флаг успешности выполнения запроса.
* `.getErrorMessages(): string[]`{lang="ts-type"} — массив сообщений об ошибках.
* `.isMore(): boolean`{lang="ts-type"} — указывает, есть ли еще данные при пагинации.

## Ключевые концепции

### Пагинация

Методы API, возвращающие списки, используют пагинацию.

* **Фиксированный размер страницы:** 50 записей.
* **Управление смещением:** Параметр `start` задает смещение.
  * `start: 0` — первая страница (записи 1-50).
  * `start: 50` — вторая страница (записи 51-100).
  * Формула: `start = (Номер_страницы - 1) * 50`.
* **Получение всех данных без пагинации:** Установите `start: -1` для эффективной выгрузки больших объемов данных. Это отключает подсчет общего количества, что значительно ускоряет работу.
  * Мы по прежнему будем получате в каждом запросе по 50 записей.
  * Идея в том что мы будем менять условие в фильтре по полю ID (`'>id': lastId`) и вызывать запрос до тех пор пока не получим пустой ответ.

### Обработка ошибок

Всегда проверяйте результат с помощью `isSuccess` и обрабатывайте ошибки.

```ts
const devMode = typeof import.meta !== 'undefined' && (import.meta.env?.DEV || import.meta.dev)
const $logger = LoggerBrowser.build('Example', devMode)

const response = await $b24.callMethod('some.method')

if (!response.isSuccess) {
  // Обработка ошибки API
  $logger.error('API Error:', response.getErrorMessages()) // @todo test ??
  return
}

// Работа с успешным результатом
const data = response.getData()
```

## Примеры

### Получение элемента CRM

```ts
import type { AjaxResult } from '@bitrix24/b24jssdk'
import { B24Hook, EnumCrmEntityTypeId, LoggerBrowser } from '@bitrix24/b24jssdk'

type CrmItem = { id: number; title: string }

const devMode = typeof import.meta !== 'undefined' && (import.meta.env?.DEV || import.meta.dev)
const $logger = LoggerBrowser.build('Example:getCrmItem', devMode)
const $b24 = B24Hook.fromWebhookUrl('https://your_domain.bitrix24.com/rest/1/webhook_code/')

async function getCrmItem(entityTypeId: number, itemId: number): Promise<CrmItem> {
  const response: AjaxResult<{ item: CrmItem }> = await $b24.callMethod('crm.item.get', {
    entityTypeId: entityTypeId,
    id: itemId
  })

  if (!response.isSuccess) {
    throw new Error(`Failed to fetch item: ${response.getErrorMessages().join('; ')}`)
  }

  return response.getData().result.item
}

// Использование для компании
const company = await getCrmItem(EnumCrmEntityTypeId.company, 42)
$logger.info(`Company: ${company?.title}`)
```

### Получение одного пользователя

```ts
import type { AjaxResult } from '@bitrix24/b24jssdk'
import { B24Hook, LoggerBrowser } from '@bitrix24/b24jssdk'

type User = { ID: number; NAME: string }

const devMode = typeof import.meta !== 'undefined' && (import.meta.env?.DEV || import.meta.dev)
const $logger = LoggerBrowser.build('Example:getUser', devMode)
const $b24 = B24Hook.fromWebhookUrl('https://your_domain.bitrix24.com/rest/1/webhook_code/')

async function getUser(id: number): Promise<User | null> {
  const response: AjaxResult<User[]> = await $b24.callMethod('user.get', { ID: id })

  if (!response.isSuccess) {
    throw new Error(`Failed to get user: ${response.getErrorMessages().join('; ')}`)
  }

  const users = response.getData().result
  return users.length > 0 ? users[0] : null
}

// Использование
const user = await getUser(1)
$logger.info(`User: ${user?.NAME} (ID: ${user?.ID})`)
```

### Получение всех данных с пагинацией

```ts

// @todo fix this all

import type { AjaxResult } from '@bitrix24/b24jssdk'
import { B24Hook, EnumCrmEntityTypeId, LoggerBrowser } from '@bitrix24/b24jssdk'

type Deal = { id: string; title: string }

const devMode = typeof import.meta !== 'undefined' && (import.meta.env?.DEV || import.meta.dev)
const $logger = LoggerBrowser.build('Example:getAllDeals', devMode)
const $b24 = B24Hook.fromWebhookUrl('https://your_domain.bitrix24.com/rest/1/webhook_code/')

async function getAllDeals(): Promise<Deal[]> {
  let allDeals: Deal[] = []
  let start = 0
  let hasMore = true

  while (hasMore) {
    const response: AjaxResult<Deal[]> = await $b24.callMethod(
      'crm.item.list',
      {
        entityTypeId: EnumCrmEntityTypeId.deal,
        order: { ad: 'asc' },
        select: ['id', 'title']
      },
      start
    )

    if (!response.isSuccess) {
      throw new Error(`Failed to fetch deals: ${response.getErrorMessages().join('; ')}`)
    }

    const pageDeals = response.getData().result
    allDeals = allDeals.concat(pageDeals)

    // Проверяем, есть ли еще данные
    hasMore = response.isMore()
    start += Number(response.getData().next) // Увеличиваем смещение для следующей страницы
  }

  return allDeals
}

// Использование
const list = await getAllDeals()
$logger.info(`Deals list (${list.length}):`, list)
```

### Эффективная выгрузка всех данных (без пагинации, start=-1)

```ts
import type { AjaxResult } from '@bitrix24/b24jssdk'
import { B24Hook, LoggerBrowser } from '@bitrix24/b24jssdk'

type Task = { id: string; title: string }

const devMode = typeof import.meta !== 'undefined' && (import.meta.env?.DEV || import.meta.dev)
const $logger = LoggerBrowser.build('Example:getAllTasksFast', devMode)
const $b24 = B24Hook.fromWebhookUrl('https://your_domain.bitrix24.com/rest/1/webhook_code/')

async function getAllTasksFast(): Promise<Task[]> {
  let allTasks: Task[] = []
  let lastId = 0
  let batch: Task[]

  do {
    const response: AjaxResult<{ tasks: Task[] }> = await $b24.callMethod(
      'tasks.task.list',
      {
        filter: { '>ID': lastId },
        order: { ID: 'ASC' },
        select: ['ID', 'TITLE']
      },
      -1 // Ключевой параметр: отключаем пагинацию и подсчет
    )

    if (!response.isSuccess) {
      throw new Error(`Failed to fetch tasks: ${response.getErrorMessages().join('; ')}`)
    }

    batch = response.getData().result.tasks
    if (batch.length > 0) {
      allTasks = allTasks.concat(batch)
      lastId = Number(batch[batch.length - 1].id) // Обновляем последний ID для фильтра
    }
  } while (batch.length > 0) // Запрашиваем, пока не получим пустой пакет

  return allTasks
}

// Использование
const list = await getAllTasksFast()
$logger.info(`Tasks list (${list.length}):`, list)
```

## Альтернативы и рекомендации

* **Для работы со списками:** Вместо ручного управления пагинацией в `callMethod` используйте:
  * [`callListMethod`](/docs/hook/methods/call-list-method/) — автоматически получает все страницы и возвращает единый результат.
  * [`fetchListMethod`](/docs/hook/methods/fetch-list-method/) — возвращает асинхронный генератор для пошаговой обработки больших списков.
* **Для групповых операций:** Используйте [`callBatch`](/docs/hook/methods/call-batch/) для выполнения до 50 команд за один запрос.
* **На клиенте (браузер):** Вместо `B24Hook` используйте встроенный объект [`B24Frame`](/docs/frame/).

## Ссылки

* **[AjaxResult](/docs/core/ajax-result/)** — Документация по объекту результата.
* **[Официальная документация REST API Bitrix24](https://apidocs.bitrix24.com/)** — Справочник всех доступных методов.
* **[Работа с большими объемами данных](https://apidocs.bitrix24.com/settings/performance/huge-data.html)** — Рекомендации по оптимизации кода для получения больших данных (использование `start=-1`).
